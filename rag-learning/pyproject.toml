[project]
name = "rag-learning"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.2",
    "sentence-transformers>=6.0.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
